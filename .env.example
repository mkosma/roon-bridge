# Roon Core connection
ROON_HOST=192.168.1.100
ROON_PORT=9100

# HTTP server
BRIDGE_PORT=3100
BRIDGE_HOST=0.0.0.0

# Auth — generate a token and share it with Claude clients
# e.g.: openssl rand -hex 32
BRIDGE_AUTH_TOKEN=changeme
