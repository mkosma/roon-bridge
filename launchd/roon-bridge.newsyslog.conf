# Log rotation for roon-bridge
# Format: logfilename [owner:group] mode count size(KB) when flags
#
# count=5  — keep 5 rotated copies
# size=1024 — rotate when file exceeds 1MB
# flags: J = compress rotated logs with bzip2
#
/Users/monty/Library/Logs/roon-bridge.log  monty:staff  644  5  1024  *  J
